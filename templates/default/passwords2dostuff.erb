#!/bin/bash

# authentication token
export OS_TOKEN="<%= node[:openstack_model_t][:admin_token] %>"

# endpoint URL
export OS_URL=http://127.0.0.1:35357/v2.0

# Password of user admin
export ADMIN_PASS="<%= node[:openstack_model_t][:ADMIN_PASS] %>"
# Database password for the Telemetry service
export CEILOMETER_DBPASS="<%= node[:openstack_model_t][:CEILOMETER_DBPASS] %>"
# Password of Telemetry service user ceilometer
export CEILOMETER_PASS="<%= node[:openstack_model_t][:CEILOMETER_PASS] %>"
# Database password for the Block Storage service
export CINDER_DBPASS="<%= node[:openstack_model_t][:CINDER_DBPASS] %>"
# Password of Block Storage service user cinder
export CINDER_PASS="<%= node[:openstack_model_t][:CINDER_PASS] %>"
# Database password for the dashboard
export DASH_DBPASS="<%= node[:openstack_model_t][:DASH_DBPASS] %>"
# Password of user demo
export DEMO_PASS="<%= node[:openstack_model_t][:DEMO_PASS] %>"
# Database password for Image service
export GLANCE_DBPASS="<%= node[:openstack_model_t][:GLANCE_DBPASS] %>"
# Password of Image service user glance
export GLANCE_PASS="<%= node[:openstack_model_t][:GLANCE_PASS] %>"
# Database password for the Orchestration service
export HEAT_DBPASS="<%= node[:openstack_model_t][:HEAT_DBPASS] %>"
# Password of Orchestration domain
export HEAT_DOMAIN_PASS="<%= node[:openstack_model_t][:HEAT_DOMAIN_PASS] %>"
# Password of Orchestration service user heat
export HEAT_PASS="<%= node[:openstack_model_t][:HEAT_PASS] %>"
# Database password of Identity service
export KEYSTONE_DBPASS="<%= node[:openstack_model_t][:KEYSTONE_DBPASS] %>"
# Database password for the Networking service
export NEUTRON_DBPASS="<%= node[:openstack_model_t][:NEUTRON_DBPASS] %>"
# Password of Networking service user neutron
export NEUTRON_PASS="<%= node[:openstack_model_t][:NEUTRON_PASS] %>"
# Database password for Compute service
export NOVA_DBPASS="<%= node[:openstack_model_t][:NOVA_DBPASS] %>"
# Password of Compute service user nova
export NOVA_PASS="<%= node[:openstack_model_t][:NOVA_PASS] %>"
# Password of user guest of RabbitMQ
export RABBIT_PASS="<%= node[:openstack_model_t][:RABBIT_PASS] %>"
# Database password of Data processing service
export SAHARA_DBPASS="<%= node[:openstack_model_t][:SAHARA_DBPASS] %>"
# Password of Object Storage service user swift
export SWIFT_PASS="<%= node[:openstack_model_t][:SWIFT_PASS] %>"
# Database password of Database service
export TROVE_DBPASS="<%= node[:openstack_model_t][:TROVE_DBPASS] %>"
# Password of Database service user trove
export TROVE_PASS="<%= node[:openstack_model_t][:TROVE_PASS] %>"
